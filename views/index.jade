extends layout

block content
  h1= title
  p Welcome to #{title}
  img(src="/images/vilop.png", class="img-logo", width="280")


  a#demoButton(href='javascript:void(0);') Push Message
  script(src='https://code.jquery.com/jquery-1.12.0.min.js')
  script.
      window.jQuery || document.write('<script src="javascripts/jquery-1.12.0.min.js"><\/script>')
  script(src='javascripts/push.js')
  script(type='text/javascript').
      $(document).ready(function() {
      Push.create('Vilop Creative', {
      body: 'Welcome to Vilop Creative',
      icon: 'images/icon.png',
      timeout: 4000,
      onClick: function() {
      console.log("Fired!");
      window.focus();
      this.close();
      },
      vibrate: [200, 100, 200, 100, 200, 100, 200]
      });
      Push.Permission.request();
      document.getElementById('demoButton').onclick = function() {
      Push.create('Vilop Creative', {
      body: 'Welcome to Vilop Creative',
      icon: 'images/icon.png',
      timeout: 4000,
      onClick: function() {
      console.log("Fired!");
      window.focus();
      this.close();
      },
      vibrate: [200, 100, 200, 100, 200, 100, 200]
      });
      };
      });

